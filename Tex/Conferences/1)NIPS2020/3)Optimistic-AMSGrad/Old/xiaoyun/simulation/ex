function [out] = ex(U)

%% extrapolation
	GradMa = [GradMa Bz];
    [rr,ll ] = size(GradMa);
    for i = 2: ll
    	t = GradMa(:,i) - GradMa(:,i-1); 
    	U = [U  t];
    end
	c = ones(dim) \ U'*U; 
	
	out = zeros(dim);
	for i = 1: ll-1
		out = out + c*GradMa(:,i);
	end

